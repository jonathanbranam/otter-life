/**
 * Project-wide constants
 */

// Screen dimensions
export const SCREEN_WIDTH = 800;
export const SCREEN_HEIGHT = 800;

// Tile system
export const TILE_SIZE = 32; // 32x32 pixel tiles
export const TILES_HORIZONTAL = SCREEN_WIDTH / TILE_SIZE; // 25 tiles
export const TILES_VERTICAL = SCREEN_HEIGHT / TILE_SIZE; // 25 tiles

// World dimensions
export const WORLD_WIDTH = 500; // 500 tiles wide
export const WORLD_HEIGHT = 500; // 500 tiles tall

// Derived values
export const SCREEN_CENTER_X = SCREEN_WIDTH / 2;
export const SCREEN_CENTER_Y = SCREEN_HEIGHT / 2;
